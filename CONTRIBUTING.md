# Contributing

Ship substantive changes through a **pull request into `main`** (feature branch → PR → merge). That keeps CI/CD, review history, and what is deployed in sync and avoids having to replay or cherry-pick work that never went through the pipeline.

Direct pushes to `main` should be reserved for emergencies only.
